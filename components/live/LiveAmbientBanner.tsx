'use client';

import React from 'react';

export const LiveAmbientBanner: React.FC = () => {
  // Top live ambient banner removed per user preference — Live Mode is accessible exclusively via the ticket
  return null;
};
